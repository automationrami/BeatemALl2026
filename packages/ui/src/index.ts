// @beat-em-all/ui — Shared shadcn-style components used by web + admin.
// Will host: Button, Card, Pill, Input, Label, Field, Toggle, Avatar, BxLogo, Pentagon stat viz, Sparkline.
// shadcn/ui primitives are added via `pnpm dlx shadcn@latest add <component>` in apps/web and apps/admin first;
// then promoted here when they need to be shared.
export const UI_PACKAGE = '@beat-em-all/ui';
