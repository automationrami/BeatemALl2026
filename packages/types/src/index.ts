// @beat-em-all/types — TypeScript types matching DOMAIN_MODEL.md.
// Will be filled in incrementally per epic. Phase 1 (E1): auth + game.

export type Locale = 'en' | 'ar';

export * from './auth';
export * from './game';
