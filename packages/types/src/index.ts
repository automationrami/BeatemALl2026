// @beat-em-all/types — TypeScript types matching DOMAIN_MODEL.md.
// Will be filled in next: User, Player, Organization, Team, Game, Venue, Booking, Challenge, Match, Tournament, Bracket, PrizePool, Notification, AuditLog.
export type Locale = 'en' | 'ar';
